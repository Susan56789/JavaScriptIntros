//initialize the array
const languages=['Java','C++','Javascript','C#','Python']

//print out the array
//console.log(languages)

//print out array length
console.log(languages.length)

//add items to the array
languages.push('Angular','C')

//display the array after adding items
//console.log(languages)

//removes the last item froom the array
languages.pop()

//removes the first item from the array
languages.shift()

//adds an item at the beginning of the array
languages.unshift('Ruby')

console.log(languages)
